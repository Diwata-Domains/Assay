# Deliverable Spec: TASK-0048

- `tests/test_verify_e2e.py` covering full verify loop with fixtures
- Validates grain payload requirements inline (no cross-product import)
- Covers: packet generation, submit, status endpoint
