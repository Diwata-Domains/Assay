# Deliverable Spec: TASK-0039

- `--export <path>` option on `assay report`
- Writes filtered packets as JSON array to the given path
- Compatible with existing `--format` modes (orthogonal, not replacing)
- Tests: `tests/test_report_export.py`
