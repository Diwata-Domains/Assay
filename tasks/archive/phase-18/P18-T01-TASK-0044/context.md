# Context: TASK-0044

Read grain's `verification_service.py` to discover the exact payload validation rules and the VERIFY-XXXX-NNN ID format. The blocking gap is that assay generates UUID verification_ids but grain requires the VERIFY-XXXX-NNN string.
