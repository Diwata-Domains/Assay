# docs/archive

Archived snapshots of working documents at key workflow boundaries.

## phases/

Snapshots of `docs/working/` state at phase close, created automatically by `grain phase close`.

Task packets for each phase are archived under `tasks/archive/phase-N/`.

## Viewing archives

```bash
grain archive list
grain archive show --phase N
```
